// Stand-in for the built dsh-connector-agent bundle. The portal only reads and
// serves the bytes, so the route's behaviour is pinned without depending on a
// build having run.
console.log('dsh-connector-agent (test fixture)')
